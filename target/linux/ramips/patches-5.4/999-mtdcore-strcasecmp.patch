--- a/drivers/mtd/mtdcore.c
+++ b/drivers/mtd/mtdcore.c
@@ -1033,7 +1033,7 @@
 	mutex_lock(&mtd_table_mutex);
 
 	mtd_for_each_device(other) {
-		if (!strcmp(name, other->name)) {
+		if (!strcasecmp(name, other->name)) {
 			mtd = other;
 			break;
 		}
