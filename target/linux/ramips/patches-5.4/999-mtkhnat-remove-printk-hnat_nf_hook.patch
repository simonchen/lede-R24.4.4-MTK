--- a/drivers/net/ethernet/mtk/mtk_hnat/hnat_nf_hook.c
+++ b/drivers/net/ethernet/mtk/mtk_hnat/hnat_nf_hook.c
@@ -1566,9 +1566,9 @@
 		else
 			entry.ipv6_5t_route.act_dp = dev->ifindex;
 	} else {
-		printk_ratelimited(KERN_WARNING
-					"Unknown case of dp, iif=%x --> %s\n",
-					skb_hnat_iface(skb), dev->name);
+		//printk_ratelimited(KERN_WARNING
+		//			"Unknown case of dp, iif=%x --> %s\n",
+		//			skb_hnat_iface(skb), dev->name);
 
 		return 0;
 	}
